diff --git a/src/features/tasks/components/dialog/task-dialog.tsx b/src/features/tasks/components/dialog/task-dialog.tsx
index 816a385..0362804 100644
--- a/src/features/tasks/components/dialog/task-dialog.tsx
+++ b/src/features/tasks/components/dialog/task-dialog.tsx
@@ -330,6 +330,41 @@ export function TaskDialog({
       ? handleWizardNext
       : save
 
+
+  const addMaterialLine = () => {
+    const current =
+      form.materials?.length
+        ? form.materials
+        : [
+            {
+              materialId: form.materialId,
+              thicknessId: form.thicknessId,
+              pieces: form.pieces || 1,
+            },
+          ]
+    update({
+      materials: [
+        ...current,
+        { materialId: "", thicknessId: "", pieces: 1 },
+      ],
+    })
+  }
+
+  // Desktop: siempre. Mobile: solo en el paso Material (último).
+  const showAddMaterial =
+    !isMobile || step === TASK_FORM_STEP_COUNT - 1
+
+  const materialFooterStart = showAddMaterial ? (
+    <button
+      type="button"
+      onClick={addMaterialLine}
+      className="inline-flex items-center gap-1.5 rounded-xl bg-foreground/5 px-3 py-2.5 text-sm font-medium text-muted-foreground transition hover:bg-foreground/10 hover:text-foreground"
+    >
+      <Plus size={15} strokeWidth={2.5} />
+      Añadir material
+    </button>
+  ) : undefined
+
   const visibleErrors =
     isMobile
       ? (stepAttempted.has(step) ? errors : undefined)
@@ -356,6 +391,7 @@ export function TaskDialog({
         subHeader={isMobile ? <TaskFormWizardProgress step={step} /> : undefined}
         onClose={close}
         onSave={footerOnSave}
+        footerStart={materialFooterStart}
       >
 
         <TaskForm
diff --git a/src/features/tasks/components/form/task-material-section.tsx b/src/features/tasks/components/form/task-material-section.tsx
index 1e3f755..76af387 100644
--- a/src/features/tasks/components/form/task-material-section.tsx
+++ b/src/features/tasks/components/form/task-material-section.tsx
@@ -1,6 +1,6 @@
 "use client"
 
-import { Package, Plus, Trash2 } from "lucide-react"
+import { Package, Trash2 } from "lucide-react"
 
 import { Input } from "@/components/ui/input"
 import { FormSection } from "@/shared/ui/dialogs/form-dialog/form-section"
@@ -8,6 +8,7 @@ import { FormField } from "@/shared/ui/dialogs/form-dialog/form-field"
 import { EntitySelect } from "@/shared/ui/entity-select/entity-select"
 import { useMaterials } from "@/features/materials/hooks/use-materials"
 import { useThicknesses } from "@/features/thicknesses/hooks/use-thicknesses"
+import { useResponsive } from "@/shared/responsive/hooks/use-responsive"
 import { cn } from "@/shared/utils/utils"
 
 import type { TaskFormSectionProps } from "./types"
@@ -18,6 +19,8 @@ export function TaskMaterialSection({
   update,
   errors,
 }: TaskFormSectionProps) {
+  const { isMobile } = useResponsive()
+
   const {
     materials,
     create: createMaterial,
@@ -34,9 +37,18 @@ export function TaskMaterialSection({
 
   const lines = form.materials?.length
     ? form.materials
-    : [{ materialId: form.materialId, thicknessId: form.thicknessId, pieces: form.pieces || 1 }]
-
-  const totalPieces = lines.reduce((s, l) => s + (Number(l.pieces) || 0), 0)
+    : [
+        {
+          materialId: form.materialId,
+          thicknessId: form.thicknessId,
+          pieces: form.pieces || 1,
+        },
+      ]
+
+  const totalPieces = lines.reduce(
+    (s, l) => s + (Number(l.pieces) || 0),
+    0,
+  )
 
   const setLines = (next: TaskMaterialLineForm[]) => {
     const primary = [...next]
@@ -50,13 +62,13 @@ export function TaskMaterialSection({
     })
   }
 
-  const updateLine = (index: number, patch: Partial<TaskMaterialLineForm>) => {
-    const next = lines.map((l, i) => (i === index ? { ...l, ...patch } : l))
-    setLines(next)
-  }
-
-  const addLine = () => {
-    setLines([...lines, { materialId: "", thicknessId: "", pieces: 1 }])
+  const updateLine = (
+    index: number,
+    patch: Partial<TaskMaterialLineForm>,
+  ) => {
+    setLines(
+      lines.map((l, i) => (i === index ? { ...l, ...patch } : l)),
+    )
   }
 
   const removeLine = (index: number) => {
@@ -64,18 +76,45 @@ export function TaskMaterialSection({
     setLines(lines.filter((_, i) => i !== index))
   }
 
+  const totalLabel = (
+    <span className="text-[11px] tabular-nums text-muted-foreground">
+      Total:{" "}
+      <span className="font-semibold text-foreground">{totalPieces}</span>{" "}
+      piezas
+    </span>
+  )
+
   return (
-    <FormSection title="Material" icon={Package}>
-      <div className="flex flex-col gap-3">
+    <FormSection title="Material" icon={Package} trailing={totalLabel}>
+      {/*
+        Scroll nativo propio: el ScrollArea del FormDialog no debe
+        capturar la rueda aquí. stopPropagation + max-height fijo.
+      */}
+      <div
+        className={cn(
+          "flex flex-col gap-2",
+          "max-h-[14rem] overflow-y-auto overscroll-y-contain",
+          "scrollbar-none [-ms-overflow-style:none] [&::-webkit-scrollbar]:hidden",
+        )}
+        onWheel={e => e.stopPropagation()}
+        onTouchMove={e => e.stopPropagation()}
+      >
         {lines.map((line, index) => {
-          const selectedMaterial = materials.find(m => m.id === line.materialId)
-          const selectedThickness = thicknesses.find(th => th.id === line.thicknessId)
+          const selectedMaterial = materials.find(
+            m => m.id === line.materialId,
+          )
+          const selectedThickness = thicknesses.find(
+            th => th.id === line.thicknessId,
+          )
 
           return (
             <div
               key={index}
               className={cn(
-                "grid grid-cols-1 gap-3 rounded-xl bg-foreground/[0.03] p-3",
+                "grid grid-cols-1 gap-3",
+                isMobile
+                  ? "rounded-xl bg-foreground/5 p-3"
+                  : "rounded-lg bg-background/70 p-2.5",
                 "tablet:grid-cols-[1fr_1fr_5.5rem_auto]",
               )}
             >
@@ -89,7 +128,9 @@ export function TaskMaterialSection({
                   items={materials}
                   placeholder="Material"
                   onChange={entity =>
-                    updateLine(index, { materialId: entity?.id ?? "" })
+                    updateLine(index, {
+                      materialId: entity?.id ?? "",
+                    })
                   }
                   onCreate={createMaterial}
                   onEdit={updateMaterial}
@@ -107,7 +148,9 @@ export function TaskMaterialSection({
                   items={thicknesses}
                   placeholder="Espesor"
                   onChange={entity =>
-                    updateLine(index, { thicknessId: entity?.id ?? "" })
+                    updateLine(index, {
+                      thicknessId: entity?.id ?? "",
+                    })
                   }
                   onCreate={createThickness}
                   onEdit={updateThickness}
@@ -149,22 +192,6 @@ export function TaskMaterialSection({
             </div>
           )
         })}
-
-        <div className="flex items-center justify-between gap-2">
-          <button
-            type="button"
-            onClick={addLine}
-            className="inline-flex items-center gap-1.5 rounded-lg px-2.5 py-1.5 text-xs font-medium text-muted-foreground transition hover:bg-foreground/5 hover:text-foreground"
-          >
-            <Plus size={14} strokeWidth={2.5} />
-            Añadir material
-          </button>
-          <p className="text-xs tabular-nums text-muted-foreground">
-            Total:{" "}
-            <span className="font-semibold text-foreground">{totalPieces}</span>{" "}
-            piezas
-          </p>
-        </div>
       </div>
     </FormSection>
   )
diff --git a/src/shared/ui/dialogs/form-dialog/form-dialog-footer.tsx b/src/shared/ui/dialogs/form-dialog/form-dialog-footer.tsx
index 37c1c49..c139f89 100644
--- a/src/shared/ui/dialogs/form-dialog/form-dialog-footer.tsx
+++ b/src/shared/ui/dialogs/form-dialog/form-dialog-footer.tsx
@@ -1,10 +1,7 @@
 "use client"
 
 import { Spinner } from "@/shared/ui/spinner/spinner"
-
-import {
-  cn,
-} from "@/shared/utils/utils"
+import { cn } from "@/shared/utils/utils"
 
 type Props = {
   canSave: boolean
@@ -14,6 +11,8 @@ type Props = {
   cancelLabel?: string
   onCancel: () => void
   onSave: () => void
+  /** Acciones a la izquierda (p.ej. Añadir material) */
+  start?: React.ReactNode
 }
 
 export function FormDialogFooter({
@@ -24,21 +23,19 @@ export function FormDialogFooter({
   cancelLabel = "Cancelar",
   onCancel,
   onSave,
+  start,
 }: Props) {
-
   return (
-
-    <div className="flex justify-end gap-3">
+    <div className="flex items-center gap-3">
+      {start && <div className="mr-auto flex min-w-0 items-center">{start}</div>}
 
       <button
         type="button"
         disabled={saving}
         onClick={onCancel}
-        className="rounded-xl bg-foreground/5 px-4 py-2.5 text-sm font-medium text-muted-foreground transition-all hover:border-border hover:bg-foreground/10 hover:text-foreground disabled:cursor-not-allowed disabled:opacity-50"
+        className="rounded-xl bg-foreground/5 px-4 py-2.5 text-sm font-medium text-muted-foreground transition-all hover:bg-foreground/10 hover:text-foreground disabled:cursor-not-allowed disabled:opacity-50"
       >
-
         {cancelLabel}
-
       </button>
 
       <button
@@ -48,7 +45,6 @@ export function FormDialogFooter({
         aria-label={saving ? (savingLabel ?? saveLabel) : undefined}
         className={cn(
           "flex min-w-38 items-center justify-center gap-2 rounded-xl px-5 py-2.5 text-sm font-semibold transition",
-
           saving
             ? "cursor-not-allowed bg-foreground/10 text-muted-foreground"
             : canSave
@@ -56,17 +52,8 @@ export function FormDialogFooter({
               : "bg-foreground/10 text-muted-foreground hover:bg-foreground/15",
         )}
       >
-
-        {saving ? (
-          <Spinner size={16} />
-        ) : (
-          saveLabel
-        )}
-
+        {saving ? <Spinner size={16} /> : saveLabel}
       </button>
-
     </div>
-
   )
-
-}
\ No newline at end of file
+}
diff --git a/src/shared/ui/dialogs/form-dialog/form-dialog.tsx b/src/shared/ui/dialogs/form-dialog/form-dialog.tsx
index 81e2d87..7722a7c 100644
--- a/src/shared/ui/dialogs/form-dialog/form-dialog.tsx
+++ b/src/shared/ui/dialogs/form-dialog/form-dialog.tsx
@@ -1,23 +1,17 @@
 "use client"
 
-import type {
-  LucideIcon,
-} from "lucide-react"
+import type { LucideIcon } from "lucide-react"
 
 import {
   Dialog,
   DialogContent,
 } from "@/components/ui/dialog"
-
-import {
-  FormDialogHeader,
-} from "./form-dialog-header"
-
-import {
-  FormDialogFooter,
-} from "./form-dialog-footer"
-
 import { ScrollArea } from "@/components/ui/scroll-area"
+import { useResponsive } from "@/shared/responsive/hooks/use-responsive"
+import { cn } from "@/shared/utils/utils"
+
+import { FormDialogHeader } from "./form-dialog-header"
+import { FormDialogFooter } from "./form-dialog-footer"
 
 type Props = {
   open: boolean
@@ -30,6 +24,8 @@ type Props = {
   cancelLabel?: string
   onCancelClick?: () => void
   subHeader?: React.ReactNode
+  /** Izquierda del footer (Cancelar/Guardar a la derecha) */
+  footerStart?: React.ReactNode
   children: React.ReactNode
   onClose: () => void
   onSave: () => void
@@ -46,62 +42,51 @@ export function FormDialog({
   cancelLabel,
   onCancelClick,
   subHeader,
+  footerStart,
   children,
   onClose,
   onSave,
 }: Props) {
+  const { isMobile } = useResponsive()
 
-  const handleOpenChange = (
-    value: boolean
-  ) => {
-
-    if (saving) {
-      return
-    }
-
-    if (!value) {
-      onClose()
-    }
-
+  const handleOpenChange = (value: boolean) => {
+    if (saving) return
+    if (!value) onClose()
   }
 
   return (
-
-    <Dialog
-      open={open}
-      onOpenChange={handleOpenChange}
-    >
-
+    <Dialog open={open} onOpenChange={handleOpenChange}>
       <DialogContent
         size="large"
-        className="flex max-h-dvh w-180 max-w-180 flex-col overflow-hidden rounded-2xl bg-popover p-0 text-foreground shadow-2xl"
+        className={cn(
+          "flex w-180 max-w-180 flex-col gap-0 overflow-hidden rounded-2xl bg-popover p-0 text-foreground shadow-2xl",
+          isMobile
+            ? "max-h-dvh"
+            : "h-[min(85vh,52rem)] max-h-[85vh]",
+        )}
       >
-
-        <FormDialogHeader
-          title={title}
-          icon={icon}
-        />
+        <div className="shrink-0">
+          <FormDialogHeader title={title} icon={icon} />
+        </div>
 
         {subHeader && (
-
-          <div className="shrink-0 px-5 py-3">
-
-            {subHeader}
-
-          </div>
-
+          <div className="shrink-0 px-5 py-3">{subHeader}</div>
         )}
 
-        <ScrollArea className="flex min-h-0 flex-1 flex-col">
-
-          <div className="px-5 py-4">
-            {children}
-          </div>
-
-        </ScrollArea>
-
-        <div className="px-5 py-4">
+        <div className="relative min-h-0 w-full flex-1 overflow-hidden">
+          <ScrollArea className="h-full w-full">
+            <div
+              className={cn(
+                "flex flex-col px-5 pb-5",
+                isMobile ? "gap-0 py-4" : "gap-5 pt-3",
+              )}
+            >
+              {children}
+            </div>
+          </ScrollArea>
+        </div>
 
+        <div className="shrink-0 border-t border-border/40 px-5 py-4">
           <FormDialogFooter
             canSave={canSave}
             saving={saving}
@@ -110,14 +95,10 @@ export function FormDialog({
             cancelLabel={cancelLabel}
             onCancel={onCancelClick ?? onClose}
             onSave={onSave}
+            start={footerStart}
           />
-
         </div>
-
       </DialogContent>
-
     </Dialog>
-
   )
-
-}
\ No newline at end of file
+}
diff --git a/src/shared/ui/dialogs/form-dialog/form-section.tsx b/src/shared/ui/dialogs/form-dialog/form-section.tsx
index 72267e8..4826eea 100644
--- a/src/shared/ui/dialogs/form-dialog/form-section.tsx
+++ b/src/shared/ui/dialogs/form-dialog/form-section.tsx
@@ -1,44 +1,55 @@
-import type {
-  LucideIcon,
-} from "lucide-react"
+"use client"
+
+import type { LucideIcon } from "lucide-react"
+
+import { useResponsive } from "@/shared/responsive/hooks/use-responsive"
 
 type Props = {
   title: string
-
   icon: LucideIcon
-
   children: React.ReactNode
+  trailing?: React.ReactNode
 }
 
+/**
+ * Desktop → patrón ExportDialog (label + panel bg-foreground/5)
+ * Mobile → form clásico (sin cambios de contrato)
+ */
 export function FormSection({
   title,
   icon: Icon,
   children,
+  trailing,
 }: Props) {
+  const { isMobile } = useResponsive()
+
+  if (isMobile) {
+    return (
+      <section className="space-y-4 border-b border-border pb-5 last:border-none">
+        <div className="flex items-center gap-2">
+          <Icon size={16} className="text-muted-foreground" />
+          <h3 className="text-xs font-bold uppercase tracking-[0.20em] text-muted-foreground">
+            {title}
+          </h3>
+          {trailing && <div className="ml-auto">{trailing}</div>}
+        </div>
+        {children}
+      </section>
+    )
+  }
 
   return (
-
-    <section className="space-y-4 border-b border-border pb-5 last:border-none">
-
-      <div className="flex items-center gap-2">
-
-        <Icon
-          size={16}
-          className="text-muted-foreground"
-        />
-
-        <h3 className="text-xs font-bold uppercase tracking-[0.20em] text-muted-foreground">
-
+    <section className="flex flex-col gap-2">
+      <div className="flex items-center justify-between gap-2 px-0.5">
+        <span className="flex items-center gap-1.5 text-[11px] font-medium uppercase tracking-wide text-muted-foreground">
+          <Icon size={14} />
           {title}
-
-        </h3>
-
+        </span>
+        {trailing}
+      </div>
+      <div className="flex flex-col gap-3 rounded-xl bg-foreground/5 p-3">
+        {children}
       </div>
-
-      {children}
-
     </section>
-
   )
-
-}
\ No newline at end of file
+}
