diff --git a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
index 44dc1be..209ebfe 100644
--- a/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
+++ b/src/features/tasks/components/expanded-row/production/task-production-panel.tsx
@@ -161,12 +161,20 @@ export function TaskProductionPanel({
         <div className="flex min-w-0 items-center gap-1.5">
 
           {StatusIcon && status && (
-            <StatusIcon size={13} style={{ color: status.color }} />
+            <StatusIcon
+              size={13}
+              className="shrink-0 text-white drop-shadow-sm"
+              style={{ color: "color-mix(in srgb, white 72%, " + status.color + ")" }}
+            />
           )}
 
           <span
-            className="truncate text-xs font-bold uppercase tracking-wide"
-            style={{ color: status?.color ?? "var(--foreground)" }}
+            className="truncate text-xs font-bold uppercase tracking-wide text-white drop-shadow-sm"
+            style={{
+              color: status
+                ? "color-mix(in srgb, white 72%, " + status.color + ")"
+                : "rgba(255,255,255,0.92)",
+            }}
           >
             {status?.name ?? "Sin estado"}
           </span>
@@ -247,8 +255,8 @@ export function TaskProductionPanel({
                               : colors.background,
                           }
                         : {
-                            backgroundColor: "rgba(255,255,255,0.03)",
-                            opacity: 0.45,
+                            // Sólido oscuro — no transparente sobre el gradiente
+                            backgroundColor: "color-mix(in srgb, var(--foreground) 14%, var(--background))",
                           }
                     }
                   >
@@ -257,7 +265,11 @@ export function TaskProductionPanel({
                     ) : (
                       <ProcessIcon
                         size={16}
-                        style={{ color: isActive ? colors.text : "var(--on-glass-faint)" }}
+                        style={{
+                          color: isActive
+                            ? colors.text
+                            : "color-mix(in srgb, var(--foreground) 45%, transparent)",
+                        }}
                       />
                     )}
                   </div>
@@ -273,9 +285,17 @@ export function TaskProductionPanel({
                       className={cn(
                         "pointer-events-none absolute -right-1.5 -top-1.5 z-10",
                         "flex h-4 min-w-4 items-center justify-center",
-                        "rounded-full bg-sky-500 px-1 text-[9px] font-bold leading-none text-foreground",
-                        "shadow-md ring-2 ring-background",
+                        "rounded-full bg-sky-500 px-1 text-[9px] font-bold leading-none text-white",
+                        "shadow-md ring-2",
                       )}
+                      style={{
+                        // Anillo del color del proceso (contraste con el centro, no negro)
+                        boxShadow: `0 0 0 2px ${
+                          isActive || isDone
+                            ? colors.backgroundActive || colors.background
+                            : "color-mix(in srgb, var(--foreground) 22%, var(--background))"
+                        }`,
+                      }}
                     >
                       {commentCount > 99 ? "99+" : commentCount}
                     </span>
@@ -288,14 +308,19 @@ export function TaskProductionPanel({
                       className={cn(
                         "pointer-events-none absolute z-10",
                         "flex h-4 min-w-4 items-center justify-center overflow-hidden",
-                        "rounded-full text-[9px] font-bold leading-none text-foreground",
-                        "shadow-md ring-2 ring-background",
+                        "rounded-full text-[9px] font-bold leading-none text-white",
+                        "shadow-md",
                         hasComments
                           ? "-bottom-1 -left-1.5"
                           : "-right-1.5 -top-1.5",
                       )}
                       style={{
                         backgroundColor: operator.color || "#404040",
+                        boxShadow: `0 0 0 2px ${
+                          isActive || isDone
+                            ? colors.backgroundActive || colors.background
+                            : "color-mix(in srgb, var(--foreground) 22%, var(--background))"
+                        }`,
                       }}
                     >
                       {operator.avatarUrl ? (
@@ -317,9 +342,16 @@ export function TaskProductionPanel({
                       className={cn(
                         "pointer-events-none absolute -right-1.5 -top-1.5 z-10",
                         "flex h-4 min-w-4 items-center justify-center",
-                        "rounded-full bg-amber-500 text-[9px] font-bold text-foreground",
-                        "shadow-md ring-2 ring-background",
+                        "rounded-full bg-amber-500 text-[9px] font-bold text-white",
+                        "shadow-md",
                       )}
+                      style={{
+                        boxShadow: `0 0 0 2px ${
+                          isActive || isDone
+                            ? colors.backgroundActive || colors.background
+                            : "color-mix(in srgb, var(--foreground) 22%, var(--background))"
+                        }`,
+                      }}
                     >
                       ?
                     </span>
@@ -332,7 +364,7 @@ export function TaskProductionPanel({
                     color:
                       isActive || isDone
                         ? colors.text
-                        : "#525252",
+                        : "color-mix(in srgb, var(--foreground) 40%, transparent)",
                   }}
                 >
                   {definition.code}
