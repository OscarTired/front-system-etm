diff --git a/src/features/engineering/components/engineering-task-row.tsx b/src/features/engineering/components/engineering-task-row.tsx
--- a/src/features/engineering/components/engineering-task-row.tsx
+++ b/src/features/engineering/components/engineering-task-row.tsx
@@ -78,10 +78,14 @@
               <span
                 title={task.project.projectCode}
                 className="shrink-0 rounded-md px-1.5 py-0.5 text-[10px] font-semibold tabular-nums tracking-wide"
-                style={{
-                  backgroundColor: `${task.project.client.color}15`,
-                  color: task.project.client.color,
-                }}
+                style={
+                  task.project.client
+                    ? {
+                        backgroundColor: `${task.project.client.color}15`,
+                        color: task.project.client.color,
+                      }
+                    : undefined
+                }
               >
                 {displayProjectCode(task.project.projectCode)}
               </span>
diff --git a/src/features/engineering/types/engineering-task.types.ts b/src/features/engineering/types/engineering-task.types.ts
--- a/src/features/engineering/types/engineering-task.types.ts
+++ b/src/features/engineering/types/engineering-task.types.ts
@@ -23,7 +23,7 @@
     id: string
     projectCode: string
     name: string
-    client: {
+    client?: {
       id: string
       name: string
       color: string
diff --git a/src/features/tasks/components/context-picker.tsx b/src/features/tasks/components/context-picker.tsx
--- a/src/features/tasks/components/context-picker.tsx
+++ b/src/features/tasks/components/context-picker.tsx
@@ -445,28 +445,30 @@
                   <SelectOption
                     key={project.id}
                     label={
-                      project.client?.name
-                        ? `${displayProjectCode(project.projectCode)} · ${project.client.name} · ${project.name}`
-                        : `${displayProjectCode(project.projectCode)} · ${project.name}`
+                      [project.client?.name, project.name]
+                        .filter(Boolean)
+                        .join(" · ") || project.name
                     }
                     icon="project"
-                    color={
-                      project.client?.color ?? "#64748B"
+                    color={project.client?.color ?? "#64748B"}
+                    leading={
+                      <span
+                        className="shrink-0 rounded-md px-1.5 py-0.5 text-[10px] font-semibold tabular-nums tracking-wide"
+                        style={{
+                          backgroundColor: `${project.client?.color ?? "#64748B"}15`,
+                          color: project.client?.color ?? "#64748B",
+                        }}
+                      >
+                        {displayProjectCode(project.projectCode)}
+                      </span>
                     }
                     selected={
-
                       mode === "both"
-
-                        ? !value.taskId &&
-                          project.id === value.projectId
-
+                        ? !value.taskId && project.id === value.projectId
                         : project.id === value.projectId
-
                     }
                     disableCheckAnimation
-                    onSelect={() =>
-                      selectProject(project.id)
-                    }
+                    onSelect={() => selectProject(project.id)}
                   />
 
                 ))}
diff --git a/src/shared/ui/select-option/select-option.tsx b/src/shared/ui/select-option/select-option.tsx
--- a/src/shared/ui/select-option/select-option.tsx
+++ b/src/shared/ui/select-option/select-option.tsx
@@ -1,6 +1,6 @@
 "use client"
 
-import { useMemo } from "react"
+import { useMemo, type ReactNode } from "react"
 import { Check } from "lucide-react"
 import { CommandItem } from "@/components/ui/command"
 import {
@@ -29,6 +29,8 @@
   icon?: EntityIcon
   color: string
   selected: boolean
+  /** Chip u otro nodo a la izquierda (ej. código de proyecto). */
+  leading?: ReactNode
   swatchColor?: string
   disableCheckAnimation?: boolean
   rightSlot?: React.ReactNode
@@ -45,6 +47,7 @@
   icon,
   color,
   selected,
+  leading,
   swatchColor,
   disableCheckAnimation,
   rightSlot,
@@ -95,7 +98,8 @@
       <div className="flex w-full items-center justify-between gap-3 min-w-0">
         {/* Sección Izquierda: Ícono + Nombre + Descripción */}
         <div className="flex min-w-0 flex-1 items-center gap-3">
-          {Icon && (
+          {leading}
+          {!leading && Icon && (
             <div
               className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg transition-all"
               style={{
