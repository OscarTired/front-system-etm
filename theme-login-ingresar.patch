diff --git a/src/app/(auth)/login/page.tsx b/src/app/(auth)/login/page.tsx
index cdf81ac..d0f3ba8 100644
--- a/src/app/(auth)/login/page.tsx
+++ b/src/app/(auth)/login/page.tsx
@@ -32,7 +32,7 @@ export default function LoginPage() {
 
           {/* LEFT SIDE */}
 
-          <div className={`hidden bg-linear-to-br from-[#0A0A0A] to-[#050505] p-4 ${SHORT}:flex ${SHORT}:flex-col ${SHORT}:justify-center laptop:flex laptop:flex-col laptop:justify-between laptop:p-8`}>
+          <div className={`hidden bg-muted p-4 dark:bg-linear-to-br dark:from-neutral-950 dark:to-black ${SHORT}:flex ${SHORT}:flex-col ${SHORT}:justify-center laptop:flex laptop:flex-col laptop:justify-between laptop:p-8`}>
 
             <div>
 
@@ -53,11 +53,11 @@ export default function LoginPage() {
 
               </div>
 
-              <p className={`text-base text-white/75 ${SHORT}:text-base laptop:text-xl`}>
+              <p className={`text-base text-foreground/80 dark:text-white/75 ${SHORT}:text-base laptop:text-xl`}>
                 COMPANY S.A.C.
               </p>
 
-              <p className={`mt-1 text-xs text-white/50 ${SHORT}:mt-1 laptop:mt-4 laptop:text-sm`}>
+              <p className={`mt-1 text-xs text-muted-foreground dark:text-white/50 ${SHORT}:mt-1 laptop:mt-4 laptop:text-sm`}>
                 Sistema de Producción
               </p>
 
@@ -72,13 +72,13 @@ export default function LoginPage() {
             */}
             <div className="hidden space-y-3 laptop:block">
 
-              <p className="text-xs text-white/45">
+              <p className="text-xs text-muted-foreground dark:text-white/45">
                 Control de proyectos · tareas · procesos · producción
               </p>
 
-              <div className="h-px w-full bg-foreground/5" />
+              <div className="h-px w-full bg-border dark:bg-white/10" />
 
-              <p className="text-xs text-muted-foreground/80">
+              <p className="text-xs text-muted-foreground dark:text-white/40">
                 Sistema interno versión 1.0
               </p>
 
@@ -135,4 +135,4 @@ export default function LoginPage() {
 
   )
 
-}
\ No newline at end of file
+}
diff --git a/src/features/processes/components/expanded-row/cards/process-editable-value.tsx b/src/features/processes/components/expanded-row/cards/process-editable-value.tsx
index 15268c3..1c4ab6b 100644
--- a/src/features/processes/components/expanded-row/cards/process-editable-value.tsx
+++ b/src/features/processes/components/expanded-row/cards/process-editable-value.tsx
@@ -107,7 +107,7 @@ export function ProcessEditableValue({
         {saving && (
           <Spinner
             size={13}
-            className="absolute right-0 text-muted-foreground"
+            className="absolute right-0 text-on-glass-muted"
           />
         )}
       </div>
@@ -140,18 +140,18 @@ export function ProcessEditableValue({
       }}
       className={
         disabled
-          ? "block w-full min-w-0 truncate cursor-default text-left font-inherit leading-inherit text-muted-foreground"
+          ? "block w-full min-w-0 truncate cursor-default text-left font-inherit leading-inherit text-on-glass-muted"
           : "block w-full min-w-0 truncate cursor-pointer text-left font-inherit leading-inherit text-inherit"
       }
     >
       {hasValue
         ? (suffix ? `${value} ${suffix}` : value)
         : (
-          <span className="block min-w-0 truncate text-muted-foreground font-normal">
+          <span className="block min-w-0 truncate font-normal text-on-glass-muted">
             {placeholder}
           </span>
         )
       }
     </span>
   )
-}
\ No newline at end of file
+}
