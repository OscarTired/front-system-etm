diff --git a/src/shared/ui/entity-chip/entity-chip.tsx b/src/shared/ui/entity-chip/entity-chip.tsx
index a897cc1..2c81eb3 100644
--- a/src/shared/ui/entity-chip/entity-chip.tsx
+++ b/src/shared/ui/entity-chip/entity-chip.tsx
@@ -13,7 +13,7 @@ type Props = {
   color?: string
   icon?: EntityIcon
   compact?: boolean
-  /** Solo icono; misma altura que el chip con texto */
+  /** Solo icono; misma altura fija que el chip con texto */
   iconOnly?: boolean
   className?: string
 }
@@ -35,12 +35,13 @@ export function EntityChip({
       title={label}
       aria-label={label}
       className={cn(
-        "inline-flex shrink-0 items-center rounded-lg font-semibold",
-        compact ? "gap-1.5 py-1 text-xs" : "gap-2 py-1.5 text-sm",
+        "inline-flex shrink-0 items-center justify-center rounded-lg font-semibold leading-none",
+        // Altura fija: iconOnly y con label miden exactamente igual
+        compact ? "h-7 gap-1.5 text-xs" : "h-8 gap-2 text-sm",
         iconOnly
           ? compact
-            ? "justify-center px-1.5"
-            : "justify-center px-2.5"
+            ? "w-7 px-0"
+            : "w-8 px-0"
           : compact
             ? "px-2"
             : "px-2.5",
@@ -51,8 +52,8 @@ export function EntityChip({
         backgroundColor: badge.background,
       }}
     >
-      {Icon && <Icon size={iconSize} />}
-      {!iconOnly && <span>{label}</span>}
+      {Icon && <Icon size={iconSize} className="shrink-0" />}
+      {!iconOnly && <span className="leading-none">{label}</span>}
     </div>
   )
 }
