diff --git a/src/features/activity-log/components/actions/activity-log-chip.tsx b/src/features/activity-log/components/actions/activity-log-chip.tsx
--- a/src/features/activity-log/components/actions/activity-log-chip.tsx
+++ b/src/features/activity-log/components/actions/activity-log-chip.tsx
@@ -3,6 +3,7 @@
 import { getActivityIcon } from "../../constants/activity-icons"
 import type { ActivityLog } from "../../types/activity-log.types"
 import { cn } from "@/shared/utils/utils"
+import { ActivityLogDetailIndicators } from "./activity-log-detail-indicators"
 import { useBadgeColors } from "@/shared/utils/use-badge-colors"
 
 type Props = {
@@ -126,6 +127,7 @@
                   Auto
                 </span>
               )}
+              <ActivityLogDetailIndicators log={log} compact />
             </div>
             {subtitle && (
               <p className="mt-0.5 truncate text-[10px] font-medium text-muted-foreground md:hidden">
diff --git a/src/features/activity-log/components/actions/activity-log-detail-indicators.tsx b/src/features/activity-log/components/actions/activity-log-detail-indicators.tsx
new file mode 100644
--- /dev/null
+++ b/src/features/activity-log/components/actions/activity-log-detail-indicators.tsx
@@ -0,0 +1,62 @@
+"use client"
+
+import { FileText, ImageIcon } from "lucide-react"
+
+import { cn } from "@/shared/utils/utils"
+import type { ActivityLog } from "../../types/activity-log.types"
+
+type Props = {
+  log: ActivityLog
+  className?: string
+  /** Mobile/compact: iconos un poco más chicos. */
+  compact?: boolean
+}
+
+/**
+ * Señales de detalle/foto sin abrir el log.
+ * Bitácora propia + supervisión.
+ */
+export function ActivityLogDetailIndicators({
+  log,
+  className,
+  compact = false,
+}: Props) {
+  const hasNote = Boolean(log.note?.trim())
+  const hasPhoto = Boolean(log.photoUrl)
+  if (!hasNote && !hasPhoto) return null
+
+  const size = compact ? 12 : 13
+  const box = compact ? "size-6" : "size-7"
+
+  return (
+    <span
+      className={cn("inline-flex shrink-0 items-center gap-1", className)}
+      onClick={e => e.stopPropagation()}
+    >
+      {hasNote && (
+        <span
+          title="Con detalle"
+          aria-label="Con detalle"
+          className={cn(
+            "flex items-center justify-center rounded-md bg-sky-500/15 text-sky-700 dark:bg-sky-500/10 dark:text-sky-400",
+            box,
+          )}
+        >
+          <FileText size={size} strokeWidth={2.25} />
+        </span>
+      )}
+      {hasPhoto && (
+        <span
+          title="Con foto"
+          aria-label="Con foto"
+          className={cn(
+            "flex items-center justify-center rounded-md bg-violet-500/15 text-violet-700 dark:bg-violet-500/10 dark:text-violet-400",
+            box,
+          )}
+        >
+          <ImageIcon size={size} strokeWidth={2.25} />
+        </span>
+      )}
+    </span>
+  )
+}
diff --git a/src/features/activity-log/components/contents/team-activity-log-page-content.tsx b/src/features/activity-log/components/contents/team-activity-log-page-content.tsx
--- a/src/features/activity-log/components/contents/team-activity-log-page-content.tsx
+++ b/src/features/activity-log/components/contents/team-activity-log-page-content.tsx
@@ -1,3 +1,4 @@
+import { ActivityLogDetailIndicators } from "../actions/activity-log-detail-indicators"
 "use client"
 
 import { useQueryClient } from "@tanstack/react-query"
@@ -118,6 +119,7 @@
             </span>
 
             <div className="flex shrink-0 items-center gap-2">
+              <ActivityLogDetailIndicators log={log} compact />
               <span className="rounded-md bg-foreground/5 px-1.5 py-0.5 text-[10px] font-medium text-muted-foreground">
                 {SHIFT_HOURS_LABEL[effectiveShift]}
               </span>
diff --git a/src/shared/ui/entity-name-label/entity-name-label.tsx b/src/shared/ui/entity-name-label/entity-name-label.tsx
--- a/src/shared/ui/entity-name-label/entity-name-label.tsx
+++ b/src/shared/ui/entity-name-label/entity-name-label.tsx
@@ -2,97 +2,61 @@
 
 import { cn } from "@/shared/utils/utils"
 
-type Props={
-  name:string
-  className?:string
-  style?:React.CSSProperties
+type Props = {
+  name: string
+  className?: string
+  style?: React.CSSProperties
 }
 
-export function EntityNameLabel({
-  name,
-  className,
-  style,
-}:Props){
-
-  const normalizedName=
-    name
-      .trim()
-      .replace(/\s+([\-\/+&])\s+/g,"$1")
-
-  const words=
-    normalizedName.split(/\s+/)
-
-  const canStack=
-
-    words.length>=2 &&
-    words.length<=3 &&
-
-    words.every(
-      word=>
-        word.length<=14
-    ) &&
-
-    normalizedName.length<=26
-
-  if(canStack){
-
-    const middle=
-      Math.ceil(
-        words.length/2
-      )
-
-    const firstLine=
-      words
-        .slice(0,middle)
-        .join(" ")
-
-    const secondLine=
-      words
-        .slice(middle)
-        .join(" ")
-
-    return(
+export function EntityNameLabel({ name, className, style }: Props) {
+  const normalizedName = name
+    .trim()
+    .replace(/\s+([\-\/+&])\s+/g, " $1 ")
+    .replace(/\s+/g, " ")
+    .trim()
+
+  const words = normalizedName.split(" ").filter(Boolean)
+
+  const singleLine =
+    !!className &&
+    (className.includes("whitespace-nowrap") ||
+      className.includes("truncate"))
+
+  const canStack =
+    !singleLine &&
+    words.length >= 2 &&
+    words.length <= 3 &&
+    words.every(word => word.length <= 14) &&
+    normalizedName.length <= 26
+
+  if (canStack) {
+    const middle = Math.ceil(words.length / 2)
+    const firstLine = words.slice(0, middle).join(" ")
+    const secondLine = words.slice(middle).join(" ")
 
+    return (
       <div
         style={style}
         className={cn(
-          "min-w-0 flex flex-1 flex-col justify-center leading-[1.05] text-sm font-medium select-none uppercase tracking-[0.02em]",
-          className
+          "flex min-w-0 flex-1 flex-col justify-center text-sm font-medium leading-[1.05] tracking-[0.02em] uppercase select-none",
+          className,
         )}
       >
-
-        <span className="min-w-0 truncate">
-
-          {firstLine}
-
-        </span>
-
-        <span className="min-w-0 truncate">
-
-          {secondLine}
-
-        </span>
-
+        <span className="min-w-0 truncate">{firstLine}</span>
+        <span className="min-w-0 truncate">{secondLine}</span>
       </div>
-
     )
-
   }
 
-  return(
-
+  return (
     <span
       style={style}
       className={cn(
-        "block min-w-0 truncate text-sm font-medium select-none uppercase tracking-[0.02em]",
-        className
+        "block min-w-0 truncate text-sm font-medium tracking-[0.02em] uppercase select-none",
+        className,
       )}
     >
-
       {normalizedName}
-
     </span>
-
   )
-
-}
\ No newline at end of file
+}
