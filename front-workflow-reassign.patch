diff --git a/src/features/workflow/hooks/use-workflow-summon.ts b/src/features/workflow/hooks/use-workflow-summon.ts
index 704458a..0da69d6 100644
--- a/src/features/workflow/hooks/use-workflow-summon.ts
+++ b/src/features/workflow/hooks/use-workflow-summon.ts
@@ -38,6 +38,11 @@ export function useWorkflowSummon() {
     onSuccess: invalidateTasks,
   })
 
+  const reassign = useMutation({
+    mutationFn: workflowService.reassign,
+    onSuccess: invalidateTasks,
+  })
+
   return {
     summon: summon.mutateAsync,
     summoning: summon.isPending,
@@ -47,6 +52,8 @@ export function useWorkflowSummon() {
     declining: declineInvite.isPending,
     unsummon: unsummon.mutateAsync,
     unsummoning: unsummon.isPending,
+    reassign: reassign.mutateAsync,
+    reassigning: reassign.isPending,
   }
 
 }
\ No newline at end of file
diff --git a/src/features/workflow/services/workflow.services.ts b/src/features/workflow/services/workflow.services.ts
index b97ee3b..ef95e98 100644
--- a/src/features/workflow/services/workflow.services.ts
+++ b/src/features/workflow/services/workflow.services.ts
@@ -195,4 +195,18 @@ export const workflowService={
 
   },
 
-}
\ No newline at end of file
+
+  /** Transfer atómico: retract previa + ASSIGN al nuevo operario. */
+  async reassign(dto:{ stepId:string; operatorId:string }){
+
+    const res=
+      await api.post<{ success:boolean; count:number }>(
+        "/workflow/reassign",
+        dto,
+      )
+
+    return res.data
+
+  },
+
+}
